<h1 class="text-center mb-3">

    <h4>{{title}}</h4>
    <ul class="list-group">
        {{#each users}}
        <li class="list-group-item">{{this._id}} : {{this.name}} : {{this.email}} : {{this.address}}</li>
        {{/each}}
    </ul>
</h1>